class Point {
  constructor(pos,vel) {
    this.pos = pos;
    this.vel = vel;
  }

  getPos() {return pos;}
  getVel() {return vel;}

  setPos(newPos) {this.pos = newPos;}
  setVel(newVel) {this.vel = newVel;}
}
